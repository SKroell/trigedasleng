# react-infinite-scroller@1.2.6 declares an old React peer range (<=16) that
# conflicts with this project's React 19. It works fine at runtime, so accept the
# peer mismatch instead of failing every install. (Replace react-infinite-scroller
# with a maintained alternative to drop this.)
legacy-peer-deps=true
